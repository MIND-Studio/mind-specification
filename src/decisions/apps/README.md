# App decisions

Decisions specific to a single app. Put each app's records in its own folder
`apps/<app>/` — e.g. `apps/todo/0001-one-doc-per-list.md`. Records here use
`scope: app:<name>`.

See [`../README.md`](../README.md) for the format and how to add one.
