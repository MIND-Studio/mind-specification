# Architecture decisions

Cross-cutting decisions about the Mind base — the pod model, the protocol, naming, the
layer model, shared domains. Records here use `scope: architecture`.

See [`../README.md`](../README.md) for the format and how to add one.
